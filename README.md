# Dockerfile template to NestJS

Basic Dockerfile template to run NestJS in a Docker container

## Setup 🚀

Build docker image

```
docker build -t docker-nestjs .
```

Run docker image

```
docker run -p 3000:3000 docker-nestjs
```
